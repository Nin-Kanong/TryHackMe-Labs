<img width="1031" height="622" alt="image" src="https://github.com/user-attachments/assets/a3ec49e5-eee5-4f17-9ccb-9cf599c9f366" /><img width="567" height="359" alt="image" src="https://github.com/user-attachments/assets/81e9b30a-1e72-4f94-8474-aba3f4728754" /><h1 align="center"> JavaScript Essentials </h1>

<img width="1529" height="253" alt="image" src="https://github.com/user-attachments/assets/5ab7a2ea-f2f5-46f5-a428-01c5446068aa" />


---

### Lab Room:

https://tryhackme.com/room/javascriptessentials


---




## Task 1: Introduction

### Connect to machine:
<img width="1125" height="353" alt="image" src="https://github.com/user-attachments/assets/2941fe38-e0f3-4487-b01c-48f8adadfe69" />


### What is JavaScript?
A scripting language that adds interactivity to websites (validation, onClick actions, animations)
Works alongside HTML (structure) and CSS (styling)
Essential for modern web development


----

## Essential Concepts

### 1. VARIABLES

Three ways to declare variables:
| **Keyword** | **Scope**       | **Use When**                                 |
| ----------- | --------------- | -------------------------------------------- |
| `var`       | Function-scoped | Older code (avoid in modern JavaScript)      |
| `let`       | Block-scoped    | When the value will change                   |
| `const`     | Block-scoped    | When the value won't change (default choice) |

Use ``const`` by default, ``let`` when you need reassignment, avoid ``var``.


### 2. DATA TYPES

Primitive Types:
- **string**: text (``"hello"``)
- **number**: integers/decimals (``42``, ``3.14``)
- **boolean**: ``true`` or ``false``
- **null**: intentional empty value
- **undefined**: variable declared but not assigned
- **object**: complex data (arrays, objects)


### 3. FUNCTIONS
Reusable blocks of code that perform specific tasks

In this we can run code on browser and direct on vscode:
````
script.js
````
````
// ================================
// Function Declaration
// ================================

// Creates a function named PrintResult.
// The function accepts one parameter called rollNum.
function PrintResult(rollNum) {
  // alert() displays a popup message.
  // The + operator joins strings together.
  alert("Username with roll number " + rollNum + " has passed the exam");
}

// ================================
// Array Declaration
// ================================

// const creates a constant variable.
// The array stores multiple roll numbers.
const rollNumbers = [101, 102, 103];

// Array contents:
//
// Index    Value
// -----    -----
// 0        101
// 1        102
// 2        103

// rollNumbers.length = 3

// ================================
// Loop Through the Array
// ================================

// Start i at 0.
// Continue while i is less than the array length.
// Increase i by 1 after every iteration.

for (let i = 0; i < rollNumbers.length; i++) {
  // Current array element
  let currentRollNumber = rollNumbers[i];

  // Pass the roll number to the function.
  PrintResult(currentRollNumber);
}
````
````
index.html
````
````
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
</body>
</html>
<script>
    function PrintResult(rollNum) {
        alert("Username with roll number " + rollNum + " has passed the exam");
    }
    const rollNumbers = [101, 102, 103];
    for (let i = 0; i < rollNumbers.length; i++) {
        let currentRollNumber = rollNumbers[i];
        PrintResult(currentRollNumber);
    }
</script>
````
<img width="1621" height="870" alt="image" src="https://github.com/user-attachments/assets/01a2d803-54cd-4885-bc9e-6266e0ad393e" />


<img width="929" height="539" alt="image" src="https://github.com/user-attachments/assets/050ebaa9-d2d2-4fb2-a383-742306b766dd" />

Explanation Table
| **Line** | **Code**                               | **Description**                                                                     |
| -------- | -------------------------------------- | ----------------------------------------------------------------------------------- |
| 1        | `function PrintResult(rollNum)`        | Defines a function named `PrintResult` that accepts one parameter called `rollNum`. |
| 2        | `alert(...)`                           | Displays a popup message showing the roll number.                                   |
| 6        | `const rollNumbers = [101, 102, 103];` | Creates an array containing three roll numbers.                                     |
| 10       | `for (let i = 0; i < 100; i++)`        | Loops from `0` to `99` (100 iterations).                                            |
| 11       | `PrintResult(rollNumbers[i]);`         | Passes the current array element to the function.                                   |


### 3. Loops


Repeat code while a condition is true
| Loop Type      | When to Use                                              |
| -------------- | -------------------------------------------------------- |
| **for**        | When you know how many times to loop                     |
| **while**      | When the loop count is unknown                           |
| **do...while** | Execute the code at least once, then check the condition |

````
// Function to print student result
function PrintResult(rollNum) {
    alert("Username with roll number " + rollNum + " has passed the exam");
}

// Prepare an array of roll numbers
const rollNumbers = [101, 102, 103];

// Loop through the array
for (let i = 0; i < rollNumbers.length; i++) {
    PrintResult(rollNumbers[i]);
}
````





### 5. REQUEST-RESPONSE CYCLE
How the web works:
- Client (Browser) → Sends request to server
- Server → Processes request
- Server → Sends response back (webpage, data, resources)
- Client → Displays the response

🔄 This is the foundation of all web communication!


---


### Question & Asnwer

#### 1. What term allows you to run a code block multiple times as long as it is a condition?
````
loop
````
<img width="950" height="124" alt="image" src="https://github.com/user-attachments/assets/ee7c9834-3b8c-44c0-b0ab-941dce051611" />


----



## Task 3: Javascript Overview

JavaScript (JS) is an interpreted language (runs directly in the browser, no compilation needed).

JS code can:
- Create variables to store data.
- Use data types (number, string, boolean, etc.).
- Make decisions with control flow (if, else, switch).
- Repeat actions with loops.
- Organize code into functions for reuse.

These are the basic building blocks for creating interactive web applications.

````
// Hello, World! program
console.log("Hello, World!");

// Variable and Data Type
let age = 25; // Number type

// Control Flow Statement
if (age >= 18) {
    console.log("You are an adult.");
} else {
    console.log("You are a minor.");
}

// Function
function greet(name) {
    console.log("Hello, " + name + "!");
}

// Calling the function
greet("Bob");
````
After in this I compile it with `node`:
````
node .\script.js
````
<img width="559" height="787" alt="image" src="https://github.com/user-attachments/assets/292ba07a-2404-40f3-970b-024077687f69" />



### Compile on Browser:
Go to `Browser` -> `Right-Click` -> `Inspect`:
<img width="1918" height="1022" alt="image" src="https://github.com/user-attachments/assets/03c659f1-a753-449d-9b9f-2239bae542d5" />

Now we can right code javascript in this:
<img width="1918" height="970" alt="image" src="https://github.com/user-attachments/assets/eaaed345-d6ce-48d1-85a4-f796254f2e6b" />


````
let x = 5;
let y = 10;
let result = x + y;
console.log("The result is: " + result);
````
<img width="1015" height="448" alt="image" src="https://github.com/user-attachments/assets/ebd74708-191b-4da7-b5eb-c731f2894d7c" />


#### Explaination:
| Code                                       | Description                                                      |
| ------------------------------------------ | ---------------------------------------------------------------- |
| `let x = 5;`                               | Creates a variable `x` and stores the value `5`.                 |
| `let y = 10;`                              | Creates a variable `y` and stores the value `10`.                |
| `let result = x + y;`                      | Adds `x` and `y` together (`5 + 10 = 15`) and stores the result. |
| `console.log("The result is: " + result);` | Prints `"The result is: 15"` to the browser's console.           |

Summary:
- ``let`` → Create a variable.
- ``=`` → Assign a value.
- ``+`` → Add numbers or join text.
- ``console.log()`` → Display output in the console.

---

### Questions & Answers

#### 1. What is the code output if the value of x is changed to 10?
````
let x = 10;
let y = 10;
let result = x + y;
console.log("The result is: " + result);
````
<img width="704" height="366" alt="image" src="https://github.com/user-attachments/assets/e9bcd000-95a4-46d3-8fbc-4c2919baa254" />

Now we see result:
````
The result is: 20
````
<img width="1248" height="106" alt="image" src="https://github.com/user-attachments/assets/b4957a7c-76cd-4a80-ad08-d90b253e0eb1" />


---


#### 2. Is JavaScript a compiled or interpreted language?
````
interpreted
````
<img width="1413" height="136" alt="image" src="https://github.com/user-attachments/assets/f5ad182e-4132-4b27-a7ca-37874ede45e8" />


Why JavaScript is interpreted
- **Runs directly**: Code executes line‑by‑line in the browser.
- **No compile step**: We don’t need to build it before running.
- **Dynamic**: Allows flexible features like changing objects at runtime.


----

## Task 4: Integrating JavaScript in HTML

- JavaScript (JS) works with HTML and CSS to make web pages dynamic and interactive.
- JS is not used to create page content—HTML does that. JS adds behavior and interactivity.
- There are 2 ways to add JavaScript to HTML:
  - Internal JavaScript – write JS inside the HTML file using the ``<script>`` tag.
  - External JavaScript – write JS in a separate ``.js`` file and link it to the HTML file.


Internal JavaScript
JS code is written inside the HTML file.
Use the ``<script>`` tag.
The ``<script>`` tag can be placed:
Inside the ``<head>`` section (runs before page content loads).
Inside the ``<body>`` section (commonly placed near the end to interact with page elements).

````
<!DOCTYPE html>
<html>
<head>
    <title>Internal JavaScript</title>
</head>
<body>

    <h1>Hello JavaScript</h1>

    <script>
        console.log("Hello, World!");
    </script>

</body>
</html>
````
| Method          | Description                                   |
| --------------- | --------------------------------------------- |
| **Internal JS** | JS code inside the HTML file using `<script>` |
| **External JS** | JS code stored in a separate `.js` file       |


<img width="1005" height="976" alt="image" src="https://github.com/user-attachments/assets/c68aab38-ba3d-4c45-bb9a-a4c608040528" />


<img width="891" height="802" alt="image" src="https://github.com/user-attachments/assets/7eb6cf17-4033-4c18-9ad2-f965cbeca2b4" />


Now we see:
````
 <!DOCTYPE html>
<html lang="en">
<head>
    <title>Internal JS</title>
</head>
<body>
    <h1>Addition of Two Numbers</h1>
    <p id="result"></p>

    <script>
        let x = 5;
        let y = 10;
        let result = x + y;
        document.getElementById("result").innerHTML = "The result is: " + result;
    </script>
</body>
</html>
````
<img width="567" height="359" alt="image" src="https://github.com/user-attachments/assets/82a120c6-de33-4805-8476-31e7b1e43c90" />


In this I want to run it on my VScode:
<img width="1497" height="950" alt="image" src="https://github.com/user-attachments/assets/df220df4-b7e8-478a-986c-0691bcd52e26" />


<img width="749" height="248" alt="image" src="https://github.com/user-attachments/assets/fa4e6af0-7a77-4517-8e8a-78105b97b1fa" />



### 1. Internal JavaScript
- JavaScript code is written inside the HTML file.
- Use the ``<script>`` tag.
- Best for small projects and learning.
- JS runs when the browser loads the page.

````
<script>
    let x = 5;
    let y = 10;
    let result = x + y;
    document.getElementById("result").innerHTML = "The result is: " + result;
</script>
````
<img width="923" height="450" alt="image" src="https://github.com/user-attachments/assets/b277281f-f463-40a5-9425-3b10e2cd35ce" />


<img width="754" height="271" alt="image" src="https://github.com/user-attachments/assets/116cf68b-c404-416b-9b97-2bab991db703" />

---

### 2. External JavaScript

- Store JavaScript in a separate .js file.
- Link the JS file to HTML using the src attribute.
- Keeps HTML clean, organized, and easier to maintain.
- Best for large projects.

````
script.js
````
````
let x = 5;
let y = 10;
let result = x + y;

document.getElementById("result").innerHTML = "The result is: " + result;
````
<img width="1378" height="361" alt="image" src="https://github.com/user-attachments/assets/6d0d1a93-dedf-48b5-a4df-09709108587b" />


````
external.html
````
````
<!DOCTYPE html>
<html lang="en">
<head>
    <title>External JS</title>
</head>
<body>

<h1>Addition of Two Numbers</h1>
<p id="result"></p>

<script src="script.js"></script>

</body>
</html>
````
<img width="943" height="548" alt="image" src="https://github.com/user-attachments/assets/0dfcfaf9-f33a-4d4d-aeef-1a9e47a9b446" />


After Compile it, and we see:
<img width="762" height="295" alt="image" src="https://github.com/user-attachments/assets/9183a199-247b-4dad-8c52-43c6c8688aae" />


#### Internal vs External JavaScript
| Feature  | Internal JS            | External JS                         |
| -------- | ---------------------- | ----------------------------------- |
| Location | Inside HTML            | Separate `.js` file                 |
| Tag      | `<script>...</script>` | `<script src="script.js"></script>` |
| Best For | Small projects         | Large projects                      |
| HTML     | Longer                 | Cleaner                             |




### Verifying Internal or External JS
Sometimes we need to determine whether a website uses Internal or External JavaScript.

Steps
- Open the webpage.
- ``Right-click`` anywhere.
- Select View Page Source (``Ctrl + U``). 
- Look for the ``<script>`` tags.

<img width="987" height="898" alt="image" src="https://github.com/user-attachments/assets/1f088ce8-3eb1-47d3-a396-87d59a67f775" />


<img width="1005" height="703" alt="image" src="https://github.com/user-attachments/assets/9c8d91cf-7a3f-48bc-837f-396cd1e64d15" />


<img width="1031" height="622" alt="image" src="https://github.com/user-attachments/assets/6f403ed2-8d8c-4305-bf41-c22f92c1e998" />

---

### Questions & Answers

#### 1. Which type of JavaScript integration places the code directly within the HTML document?
````
Internal
````
<img width="1657" height="203" alt="image" src="https://github.com/user-attachments/assets/a87336b8-2f71-4b6a-afbf-b9251e57dea6" />



#### 2. Which method is better for reusing JS across multiple web pages?
````
External
````
<img width="1551" height="195" alt="image" src="https://github.com/user-attachments/assets/0e59bd08-36ba-4cc4-9402-ac61a664e8c8" />

#### 3. What is the name of the external JS file that is being called by external_test.html?
<img width="853" height="415" alt="image" src="https://github.com/user-attachments/assets/a8f88b2e-a73f-4885-94e7-11c243b7e2e2" />

Now we see answer is:
````
thm_external.js
````
<img width="1367" height="158" alt="image" src="https://github.com/user-attachments/assets/89ebcf29-589a-4e96-a702-1fabd77651b9" />



#### 4. What attribute links an external JS file in the <script> tag?
````
src
````
<img width="1299" height="154" alt="image" src="https://github.com/user-attachments/assets/af6dad50-628a-4a23-b5a6-0b82e99dfd61" />


----


























